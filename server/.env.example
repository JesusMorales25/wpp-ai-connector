# Variables de entorno para Railway (WhatsApp Server)
# Configura estas variables en el dashboard de Railway

# Puerto (Railway asigna automáticamente)
PORT=3001

# URL del frontend para CORS (tu app en Vercel)
FRONTEND_URL=https://tu-app.vercel.app

# URLs permitidas para CORS (separadas por comas)
ALLOWED_ORIGINS=https://tu-app.vercel.app,http://localhost:8080

# Configuración de WhatsApp
WHATSAPP_SESSION_PATH=/tmp/session_data

# Configuración de seguridad
NODE_ENV=production

# API Key para seguridad (debe coincidir con VITE_BOT_API_KEY del frontend)
BOT_API_KEY=tu-api-key-secreta

# Nivel de logs - Controla TODOS los logs (Baileys + App)
# silent: Sin logs (solo errores de consola) - AHORRO MÁXIMO 💰
# error: Solo errores críticos (recomendado para bajo presupuesto)
# warn: Errores + advertencias (recomendado para producción)
# info: Información importante (default producción)
# debug: Logs detallados (default desarrollo)
# trace: Absolutamente todo (solo debugging temporal)
LOG_LEVEL=silent